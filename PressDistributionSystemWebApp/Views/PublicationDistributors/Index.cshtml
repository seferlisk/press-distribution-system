@model PressDistributionSystemWebApp.DTO.PublicationDistributionIndexDTO

@{
    ViewData["Title"] = "Index";
}

<form asp-action="Index">
    <div>
        @Html.HiddenFor(model => model.Publication.Id)
        @Html.HiddenFor(model => model.Publication.Name)
        @Html.HiddenFor(model => model.Publication.Issue)
        @Html.HiddenFor(model => model.Publication.Quantity)

        <h1>@Html.DisplayFor(model => model.Publication.Name)</h1>
        <h2>Issue no: @Html.DisplayFor(model => model.Publication.Issue)</h2>
        <h5>Available quantity: @Html.DisplayFor(model => model.Publication.Quantity)</h5>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>
                    Distributor
                </th>
                <th>
                    Quantity
                </th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.PublicationDistributors.Count; i++)
            {
                <tr>
                    <td>
                        @Html.HiddenFor(model => Model.PublicationDistributors[i].Id)
                        @Html.HiddenFor(model => Model.PublicationDistributors[i].DistributorId)
                        @Html.HiddenFor(model => Model.PublicationDistributors[i].DistributorName)
                        @Html.DisplayFor(model => Model.PublicationDistributors[i].DistributorName)
                    </td>
                    <td>
                        @Html.TextBoxFor(model => Model.PublicationDistributors[i].Quantity)     
                        @Html.ValidationMessageFor(model => Model.PublicationDistributors[i].Quantity, "", new { @class = "text-danger" })

                    </td>
                </tr>
            }
        </tbody>
    </table>
    <div class="form-group">
        <input type="submit" value="Submit" class="btn btn-primary" />
    </div>
</form>

